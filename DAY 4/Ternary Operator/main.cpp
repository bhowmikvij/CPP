//  if/else questions can also be done through ternary operators

#include<iostream>
using namespace std;

int main(){

    // int age = 22;
    // (age > 18) ? cout << "Can vote" : cout << "Cannot vote" ;

    int x = 10;
    int y = 20;

    int result = (x>y) ? x:y;
    cout << "Result: " << result << endl; // 20

    return 0;
}


